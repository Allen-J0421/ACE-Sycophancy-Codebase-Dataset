# Task T01

First read test/tasks/COMMON.md and follow it exactly.

## Units
- 024_floyd_warshall: ends as FloydWarshall/Graph/ShortestPaths/Vertices (+ its own TestAssertions/Main). Oracle: reference Floyd-Warshall on random dense graphs, negative edges allowed but no negative cycles unless the baseline defines their handling. Match the baseline's INF representation exactly.
- 025_kruskals_mst: compare MST total weight against a reference MST (use distinct weights if you also compare edge sets). Check what the baseline does on a disconnected graph before you test one.
- 027_connected_components: ends as src/main/java/graph/ with BFS/DFS TraversalStrategy. Compare components as an order-independent partition against a reference union-find, and exercise every exposed traversal strategy.
