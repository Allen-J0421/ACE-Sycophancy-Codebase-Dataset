# Task T04

First read test/tasks/COMMON.md and follow it exactly.

## Units
- 040_bubble_sort: ends as src/main/java/sorting/ with several sorters and SortObserver instrumentation. Test the bubble sort contract against Arrays.sort; you may also cover the other sorters if they are the same shape. Also check any exposed SortStats aren't nonsensical (e.g. negative).
- 041_quickselect: check the baseline's k convention (0- or 1-based, smallest or largest). Compare against a sorted reference over many (array, k) pairs, including duplicates, and check the input array is still a permutation afterwards.
- 042_balanced_parentheses: table-driven oracle over the baseline's bracket set: balanced, unbalanced, wrong nesting, empty, and non-bracket chars (if the baseline allows them).
- 043_circular_queue: ends as CircularDeque. Model-test against ArrayDeque plus a capacity bound. Match the baseline's full/empty behavior (exception vs sentinel vs silent) exactly, and exercise wraparound heavily.
