# Task T05

First read test/tasks/COMMON.md and follow it exactly.

## Units
- 044_avl_tree_insertion: ends as AVLTree/Main. Oracle: the inorder traversal equals the sorted inserted keys (baseline duplicate policy), plus the AVL balance invariant through reflection on node fields. Also compare the preorder output for the baseline's demo sequence.
- 045_red_black_tree_insertion: ends as the rbtree/ package (Color/Node/RedBlackTree/TreePrinter). Check inorder == sorted keys, root is black, no red-red, and equal black-height, via reflection.
- 046_b_tree: check the degree parameter/constructor drift. Oracle: traversal == sorted keys, search hit/miss, and the node key-count bounds for the degree used.
