# Task T06

First read test/tasks/COMMON.md and follow it exactly.

## Units
- 047_cutting_a_rod: reference DP optimal revenue over 30+ random price arrays plus the baseline demo.
- 048_naive_pattern_searching: ends as NaivePatternSearch. Occurrence indices vs an indexOf scan, including overlapping matches, pattern longer than the text, and the empty pattern ONLY if the baseline defines it.
- 049_euclidean_algorithms: gcd (and extended gcd if present: verify the Bezout identity) vs BigInteger.gcd, including zeros. Match the baseline's negative-number behavior.
- 050_modular_exponentiation: vs BigInteger.modPow within the baseline's domain. Mind int/long overflow in the baseline; don't test outside what it supports, but DO flag it if a later iteration narrows the domain.
