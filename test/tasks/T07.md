# Task T07

First read test/tasks/COMMON.md and follow it exactly.

## Units
- R001_module_java: iteration 10 repackaged everything (app/, config/, events/, logging/, model/). Check determinism at the baseline (run it twice in-process for ~50 steps). If deterministic, use a golden per-step population trace; otherwise a smoke/property oracle.
- R002_module_java: safari+weather sim (Organism->Animal/Plant). NON-deterministic, so use a property/smoke oracle: 100+ steps, repeated 3x, no exceptions, non-negative populations, and the sim progresses.

See test/units/R009_module_java (golden trace) and R010_module_java (smoke) as the two reference R drivers. Keep the runner's -Djava.awt.headless=true. If the Swing view throws HeadlessException, drive the model layer (Field + populate + step).
