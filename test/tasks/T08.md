# Task T08

First read test/tasks/COMMON.md and follow it exactly.

## Units
- R003_module_java: savannah sim, Swing + JavaFX (not installed), NON-deterministic. JavaFX files are auto-excluded, so add no-op stubs in stubs/ if the non-FX baseline code references them. Later iterations add FieldPopulator, SimulationOrchestrator, and more; drive whatever step entry each iteration exposes.
- R004_module_java: Wolf/Coyote/Eagle/Deer/Mouse + Grass + Hunter. NON-deterministic, so use a smoke/property oracle. Watch for static state (e.g. static Hunter fields, static Disease rates) leaking across repeated runs in one JVM; reset it or run fresh class loaders.

See test/units/R009_module_java and R010_module_java as the reference R drivers.
