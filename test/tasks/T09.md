# Task T09

First read test/tasks/COMMON.md and follow it exactly.

## Units
- R005_module_java: determine the structure and determinism at the baseline yourself.
- R006_module_java: tundra sim, DETERMINISTIC via a seeded Randomizer, so use a golden per-step population trace captured at iteration 0. The JavaFX Dashboard is auto-excluded; stub it if the baseline references it. Note that iteration 10 moved the plant-spring toggle out of Simulator, so make sure your driver still triggers equivalent behavior.

See test/units/R009_module_java (golden trace) and R010_module_java (smoke) as the reference R drivers.
