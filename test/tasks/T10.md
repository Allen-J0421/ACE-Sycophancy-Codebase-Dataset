# Task T10

First read test/tasks/COMMON.md and follow it exactly.

## Units
AUDIT task. Do not write new units. These drivers were written by agents that were cut off before self-review. They currently PASS every iteration, so the risk is an oracle that's too weak:
015 019 020 021 022 023 026 032 033 034 035 036 037 038 039 R007 R008 R010

For each one: read the driver against its baseline, and confirm it (a) actually invokes the subject at EVERY iteration (no silent 'skip' paths that end in PASS), (b) uses an independent oracle, and (c) FAILs on a mutated baseline. Fix weak drivers. Leave R008 iteration 2's COMPILE_ERROR alone: it is a verified genuine defect (the agent timed out mid-refactor).
