/**
 * Placeholder so the runner has at least one compilable source after
 * prefix_sum_array.java is excluded (see unit.conf). The Driver compiles the
 * real subject source itself, working around the baseline's public-class /
 * filename mismatch. This class is inert and never matched by discovery.
 */
final class UnitCompilePlaceholder {
    private UnitCompilePlaceholder() {}
}
