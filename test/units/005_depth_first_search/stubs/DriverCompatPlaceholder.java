/**
 * Infrastructure placeholder for 005_depth_first_search. The subject file is
 * excluded from the runner's javac pass (see unit.conf: the baseline's public
 * class name does not match its file name), so this stub keeps the runner's
 * "no compilable sources" check satisfied. The Driver compiles the real
 * subject sources itself with filename-compatible staging.
 */
final class DriverCompatPlaceholder {
    private DriverCompatPlaceholder() { }
}
